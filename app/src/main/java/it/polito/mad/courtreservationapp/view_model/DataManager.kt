package it.polito.mad.courtreservationapp.view_model


object DataManager{

    init {
        //some init if needed
    }
    var variableName = "I am Var"
    fun printVarName(){
        println(variableName)
    }
}